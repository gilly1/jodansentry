name: Deployment

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/jodan-sentry:latest
            ${{ secrets.DOCKER_USERNAME }}/jodan-sentry:${{ github.sha }}

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_DIR }}/jodan
            docker compose pull app queue scheduler
            docker compose up -d app queue scheduler
            docker exec mpesa_gateway php artisan migrate --force
            docker exec mpesa_gateway php artisan config:cache
            docker exec mpesa_gateway php artisan route:cache
            docker exec mpesa_gateway php artisan view:cache
            docker exec mpesa_gateway php artisan queue:restart
